# Postgres is internal-only in deploy/docker-compose.yml (service postgres).
# DB admin UI: pgAdmin at http://localhost:${PGADMIN_PORT:-5052}
#
#   docker compose -f deploy/docker-compose.yml up -d postgres pgadmin
