Optimize the Python code below for performance and efficiency.

Rules:
- Replace any remaining Python loops with vectorized pandas/numpy operations
- Remove redundant computations
- Use context managers for file operations
- Do NOT change function signatures or logic
- Do NOT add new imports unless strictly necessary

```python
{{code}}
```

List each change you made as:
CHANGE: <description>

Then return the optimized code inside a ```python ... ``` block.
