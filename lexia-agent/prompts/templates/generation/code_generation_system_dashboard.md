DASHBOARD HTML MODE: Your generated code MUST create a standalone HTML dashboard artifact instead of a plain stdout-only report. Use the helper module `prompts.skills.dashboard_html.report_builder` so the output is based on the official BI templates. Embed KPI cards, charts, and tables inside the HTML. Charts must be provided as JSON-serializable ECharts option dictionaries. At the end of execution, print a concise French summary and then print a final line formatted exactly as `HTML_REPORT_PATH: /absolute/path/to/report.html`.
